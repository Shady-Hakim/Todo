import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: { 
        alignItems:'center'
    },
    title: {
        fontSize:30,
        marginTop:40,
        textAlign:'center'
    },
    subTitle: {
        fontSize:20,
        marginTop:40,
        textAlign:'center'
    }
});
export default styles;
